//#include <iostream>
#include <stdio.h>
#include <string>

using RUB = unsigned long int;

bool stipend = true;

int month = 1;
int year = 2026;

int end_month = 7;
int end_year = 2036;

RUB my_profit = 5'820;
RUB money = 10'000;

//=========================================================================

int my_money_count()                       // Выписка бюджета
{
    printf("%d\n", money);
    return 0;
}

//=========================================================================

int my_money_profit(RUB profit)            // Изменение бюджета
{
    money += profit;
    return 0;
}

//=========================================================================

int my_salary()
{
    if (stipend){
        my_money_profit(5'820);
    }
}

//=========================================================================


int main()
{
    for(year; year <= 2036; year++){          // Ежегодно

//        printf("%d\n", year);

        for (month; month < 13; month++){     // Ежемесячно
                if(year == end_year && month == end_month){
                    break;
                }

//              printf("--%d\n", month);
                my_salary(); 

        }
        month = 1;
    }
    my_money_count();
}

